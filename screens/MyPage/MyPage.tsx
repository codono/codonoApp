import * as React from 'react';
import { Text, SafeAreaView } from 'react-native';

export const MyPage = () => {
  return (
    <SafeAreaView>
      <Text>mypage screen</Text>
    </SafeAreaView>
  );
};
